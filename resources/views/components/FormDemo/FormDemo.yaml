with:
    header:
        component: FormHeader
        with:
            action: '/styleguide/formdemo'
            method: post
    textfield:
        component: FormTextfield
        with:
            name: textfield
            label: Label
            value: Value
    textarea:
        component: FormTextarea
        with:
            name: textarea
            label: Label
            value: Value
    select:
        component: FormSelect
        with:
            name: select
            label: Label
            options: 
                - Some
                - Two
                - Three
    checbox:
        component: FormCheckbox
        with:
            name: checkbox
            label: Checkbox
            checked: false
    button:
        component: FormButton
        with:
            title: Title
    footer:
        component: FormFooter
        with:
